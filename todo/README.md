# ToDo

```
$ cargo new todo

$ cd todo

$ cargo add actix-web actix-rt
error: no such subcommand: `add`
	Did you mean `b`?

$ cargo addがcargo-eidtをインストール必要がある

$ cargo install cargo-edit

$ cargo add actix-web actix-rt
```

### run
```
$ cargo install cargo-watch

$ ./run-development.sh

$ ./run-production.sh
```
